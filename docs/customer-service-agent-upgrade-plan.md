# 客服 Agent 升级计划

> 版本：1.0
> 日期：2026-09-20
> 状态：已实施并部署（v1.0.14-local-20260921）
> 关联代码：`internal/engine/aimatch`、`internal/engine/ai_scope.go`、`internal/engine/ai.go`、设置页「AI 客服配置」编辑区

## 一、背景与问题

系统自带的 AI 自动回复只接管「砍价意图」的消息，其余消息走关键词/默认回复。

- 接管逻辑写死在 `internal/engine/ai_intent.go`：意图正则与接管策略都在代码里，改动要发版。
- 只有砍价消息触发 AI，买家常问的资源内容、发货方式、库存/在售、政策类问题都被旧机制处理，体验生硬。
- 负向词表（退款/退货/投诉等）偏窄，IM 里实际出现过的「违规、扣分、封号、申诉」等说法拦不住。

**需求**：AI 客服的边界、策略、语料都要能低代码配置（改配置即时生效，不写代码、不重启），并把 AI 的接管范围从「仅砍价」扩展为与店铺业务匹配的合理范围。

## 二、目标与范围

1. 统一判定语言：用「组合词」布尔表达式替代硬编码正则，一处语法三处复用。
2. 三层可配置：边界（哪些意图交给 AI）、语料（FAQ 与在售清单）、策略（报价话术）。
3. 向后兼容：未配置或配置非法时回落内置行为（仅砍价接管），历史行为不变。
4. 有据可答：FAQ 命中的店规与在售清单注入 AI 提示词，回答以店铺知识为依据。

不包含（后续另行规划）：语义向量检索、槽位抽取与多轮流程、投诉/纠纷自动处理。

## 三、整体设计

### 3.1 组合词语法（aimatch 包）

表达式由词与运算符组成，低代码友好：

| 符号 | 含义 | 示例 |
|---|---|---|
| `\|` | 或 | `(便宜 \| 优惠)` |
| `&` | 且 | `(砍价 & 元)` |
| 相邻词 | 默认「且」 | `百度网盘 发货` |
| `!` | 非 | `夸克 & !会员` |
| `( )` | 分组 | `(砍价 \| 优惠) & (元 \| 块)` |
| `"..."` | 正则词 | `"第[一二三0-9]季"`（用于变动的数字） |

- 普通词按「包含」匹配，忽略大小写；括号、竖线等符号不用转义。
- 正则词必须用双引号包裹，内部 `(` `|` 不作为语法符号。
- 空表达式恒不命中；语法/正则非法时返回错误，配置侧按「忽略该项或整体回落」处理。

### 3.2 三层配置（存于系统设置 system_settings）

| 配置键 | 层 | 内容 |
|---|---|---|
| `ai_scope_config` | 边界 | `intents`（意图白名单：id/enabled/ai/match）+ `negative`（负向组合词） |
| `ai_knowledge_config` | 语料 | `faq`（问答知识）+ `catalog`（在售清单） |
| `ai_policy_config` | 策略 | `min_price_reply`（最低价话术，`{amount}` 占位）+ `no_discount_reply`（拒绝话术） |

判定顺序（消息进来后）：

1. 负向组合词命中（含内置词表 ∪ 配置 negative ∪ 扩展词表 `ai_complaint_keywords`）→ 一票否决，交给默认回复/人工。
2. 依次匹配启用的意图：`enabled=false` 忽略；`ai=false` 只记录命中（写入对话历史标签）不接管。
3. 任一 `ai=true` 的意图命中 → AI 接管；否则走默认回复。
4. 接管后：命中 FAQ 的答案与在售清单注入 system prompt；报价超出边界时套用策略话术。

配置读取规则：每次回复从 settings 读取（与既有 ai_api_key 读取方式一致），改配置即时生效，无需重启。JSON 非法、意图词表非法或无可用意图时回落内置。

### 3.3 内置默认（未配置时）

等价于历史行为：仅 `bargain` 意图可接管；负向词为内置投诉词表；询价/订单/咨询意图仅参与命中标注（保证「多意图归并 ambiguous」等历史语义不变）。

## 四、已实施清单

| 项 | 位置/说明 |
|---|---|
| 组合词匹配器 | `internal/engine/aimatch/expr.go`（分词 + 递归下降解析 + 求值），配套聚焦测试 |
| 三层配置读取与回退 | `internal/engine/ai_scope.go`（parse + compile + 默认回落），配套测试 |
| AI 接管策略改造 | `internal/engine/ai.go`（decide 驱动接管、FAQ/清单注入 prompt、策略话术替换） |
| 前端配置编辑区 | 设置页「AI 客服配置」：三个 JSON 编辑框 + 组合词语法说明 + 本地 JSON 校验 |
| 静态产物 | `internal/webui/static` 随前端构建刷新（Docker 构建时重新生成并嵌入） |
| 扩展负向词 | 内置词表并入配置化 negative（含 违规/扣分/封号/申诉/拉黑 等），保留 `ai_complaint_keywords` 扩展位 |

## 五、IM 梳理与接管范围依据

对账号 11873716 的 430 条入站消息 / 56 个会话做分类梳理，买家常问问题与 AI 适用范围如下：

| 类型 | 代表话术（摘录） | 是否交给 AI |
|---|---|---|
| 砍价/议价 | 「能便宜点吗」「130块我就都要了」 | 是（原有） |
| 资源/商品内容 | 「是正版的吗」「有音频嘛」「文字版在哪里」 | 是（新增 product_qa） |
| 发货/网盘兼容 | 「百度网盘发货吗」「有夸克的吗」「资源码怎么用」 | 是（新增 delivery） |
| 库存/在售 | 「有第三季了吗」「可以单买第二季吗」「全集吗」 | 是（新增 stock，依据在售清单） |
| 售后/退款/纠纷 | 「那我退了吧」「我要退款」 | 否（负向拦截） |
| 账号风控敏感 | 「被举报了」「扣了20」「不能挂永久」 | 否（负向拦截，绝不 AI） |
| 商务/广告/合作 | 网盘拉新推广、货源对接 | 否（人工） |
| 闲聊 | 「在吗」「谢谢」 | 否（维持默认回复） |

## 六、上线种子配置（已写入生产库）

- 边界：接管 砍价 + 商品咨询 + 发货 + 库存 四类；负向词含 退款/退货/投诉/差评/举报/骗子/骗人/假货/被骗/维权/违规/扣分/封号/申诉。
- 语料：FAQ 五条（发货方式、资源码/鸿蒙、退款政策、包更新承诺、好评赠送）+ 在售清单 22 条（从 `item_info` 自动生成）。
- 策略：`min_price_reply` / `no_discount_reply` 采用内置话术明文写入（便于页面可见可改）。

账号级「启用 AI 自动回复」开关（`ai_reply_settings.ai_enabled`）与全局模型/APIKey 配置仍需在页面确认开启，二者是 AI 参与对话的前置条件。

## 七、验证证据

- Go：`engine / engine/aimatch / automation / heartbeat` 测试全绿；`go vet ./...` 通过。
- 注释门禁：`commentlint` 全仓通过（含补齐两处既有违规）。
- 前端：`tsc --noEmit` 通过；533 个单测全绿；`vite build` 成功、静态产物刷新。
- 端到端聚焦测试 `TestAIReplyScopeKnowledgeIntegration`：配置的 delivery 意图接管并写入对应意图标签、负向词拦截、未配置意图不接管。
- `tools/architecturecheck` 存在一条与本任务无关的 HEAD 遗留违规（`internal/engine/account.go` 806 行超过 800 行上限），未在本任务处理。

## 八、提交与发布

- 分支 `feat/ai-scope-config` 四笔提交：组合词匹配器 / 三层配置接入 / 前端编辑区+静态产物 / 注释违规补齐。
- 合并 `--no-ff` 到 main 并推送 fork（提交 ed8061b 及其上游）。
- 版本 tag：`v1.0.14-local-20260921`；旧镜像备份 `rollback-20260921`。
- 部署状态：见文末「部署记录」更新。

## 九、遗留与后续

1. 部署阻塞：Docker VM 磁盘不足 + debian 源下载慢，`install-deps chromium` 失败，新镜像未替换成功（待解决后 `docker compose up -d app`）。
2. 可考虑把 Dockerfile runtime 阶段 apt 源切换到国内镜像（镜像构建网络受限时与 goproxy.cn 同理）。
3. 后续增强方向（参考课程 V4 客服 Agent）：语义检索（Milvus）、槽位与多轮 SOP（换货/改地址/开票）、三级护栏、评测集。

---

## 十、部署记录

### 2026-09-21 异地恢复部署

- 依据 `backup/README.md` 恢复：`.env`（原样保留密钥）、PostgreSQL 备份导入、`app_data`/`browser_data` 数据卷还原，账号登录态保留。
- 旧补丁：GHCR 正式镜像 `latest`（v1.0.13，2026-09-17 构建）**不含**「AI 客服配置」功能，设置页无该卡片。
- 处理：Dockerfile.debian13 本地构建 `ydisks-xianyu-helper:local`（v1.0.14-local，含 AI 客服功能），`compose.override.yml` 指向本地镜像并禁用拉取；旧 GHCR 镜像备份为 `rollback-20260921`。
- 构建阻塞与解决：
  - Docker Hub buildkit 拉取 golang 层极慢（0.3 MB/s 级）：开 VPN 后恢复（基础镜像层拉取即快）。
  - Playwright FFmpeg 下载失败：重试通过。
  - `install-deps chromium` 多次因 apt 包下载失败：实测 aliyun 镜像对大包降速至 0.3 MB/s（libllvm19 23MB 拉不动），清华 tuna 41 MB/s；已把 Dockerfile runtime 阶段 apt 源从 aliyun 切到清华（http，未加 https 需 CA），单次约 80 秒下载完成。
- 验证：`/health` healthy，version=v1.0.14-local；浏览器实测设置页「AI 客服配置（可配置接管范围）」卡片渲染，边界/语料/策略数据从 API 加载完整（意图 4 条、FAQ 5 条、在售清单 22 条）。

## 十一、评测驱动优化记录（2026-09-21）

决策层评测集 v1 与内容层评测落地后，对升级计划第五节真实话术做了复现核对，结论如下：

1. **数字报价匹配非缺陷（澄清）**：内置砍价组合词 `builtinBargainExpr` 中的 `\\d` 位于普通 Go 字符串内，编译后即为 `\d`，转义正确；实测「20块卖吗」「30元可以吗」均正常命中。此前疑似「双反斜杠导致不匹配」为误报，无需改动。
2. **真实口语负向漏拦（已修复）**：真实话术「那我退了吧」「扣了20」不含连续子串「退款」「扣分」，现有负向词表（内置 + 生产种子配置）均拦不住。评测集新增 `aiEvalSpecialRealPhrase` 标注锁定这两条用例，并将生产种子配置负向词表追加 `退了|扣了`。**生产库 `ai_scope_config` 的 negative 需同步追加 `退了|扣了` 后才对线上生效**；本机评测集仅代表期望基线。
